public interface Interface {
    public static final String EXPLICIT = "Hello";
    String IMPLICIT = "World";
    double PI_CONST = Math.PI;
    // This is a comment.
    void abstractMethod(String param, double d);
}
